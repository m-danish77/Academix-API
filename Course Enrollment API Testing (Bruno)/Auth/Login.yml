info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "mmunibdanish1@gmail.com",
        "password": "1234"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        // Parse the response body
        const responseData = res.getBody();

        // Access the token from the top level of your object
        if (responseData.token) {
            bru.setEnvVar("token", responseData.token);
            console.log("Success: Token has been updated in the environment.");
        } else {
            console.warn("Script Warning: No token found in the response body.");
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
