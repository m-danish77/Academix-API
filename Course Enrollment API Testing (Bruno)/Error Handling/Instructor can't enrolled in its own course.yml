info:
  name: Instructor can't enrolled in its own course
  type: http
  seq: 3

http:
  method: POST
  url: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
