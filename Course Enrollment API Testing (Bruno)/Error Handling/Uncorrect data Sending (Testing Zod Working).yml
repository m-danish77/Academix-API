info:
  name: Uncorrect data Sending (Testing Zod Working)
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/courses"
  body:
    type: json
    data: |
      {
         "title": "Zod Learning course",
         "description": "Learn Zod in Depth",
         "maxCapacity": 5,
         "price": "200"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
