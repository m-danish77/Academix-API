info:
  name: Delete Course (Admin or Instructor who created it)
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/api/courses/6a37d08d886d5cc9d207581f"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
