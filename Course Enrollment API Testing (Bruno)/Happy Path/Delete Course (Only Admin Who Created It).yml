info:
  name: Delete Course (Only Admin Who Created It)
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/api/courses/69fb38bbf004601dabeef757"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
