info:
  name: Get All Courses of Specific Students (Admin)
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/my-courses?studentId=69fb0a5b9bccdec931de97f6"
  params:
    - name: studentId
      value: 69fb0a5b9bccdec931de97f6
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
