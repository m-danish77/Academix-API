info:
  name: Get All Courses of Specific Students (Student)
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/my-courses"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
