info:
  name: Get All Courses of Specific Students
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/my-courses"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
