info:
  name: Get All Courses
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/courses"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
