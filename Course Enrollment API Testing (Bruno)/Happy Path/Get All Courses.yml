info:
  name: Get All Courses
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/courses?pagesds=2&limit=1"
  params:
    - name: pagesds
      value: "2"
      type: query
    - name: limit
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
