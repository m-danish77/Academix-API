info:
  name: Get Specific Course Enrolled Students (Admin)
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/course/69fb32a5a542675ea0d7237b/students"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
