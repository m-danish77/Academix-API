info:
  name: Update Course (Admin or Instructor Who Created It)
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/api/courses/69fb32a5a542675ea0d7237b"
  body:
    type: json
    data: |-
      {
         "title": "Advanced JS",
         "description": "Learn JS in Depth",
         "maxCapacity": 9,
         "price": 200
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
